import type { Resource } from "@/app/lib/types";

// Resources are yours to add — job posts, educational links, social/website
// links. Stored in Supabase (synced across devices). Starts empty on purpose;
// add your own from the Resources page.
export const SEED_RESOURCES: Resource[] = [];

// Weekly goal target (hours) shown in the resources dashboard.
export const WEEKLY_GOAL_HOURS = 10;
